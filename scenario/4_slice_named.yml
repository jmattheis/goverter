input:
    input.go: |
        package slices

        // genconv:converter
        type Converter interface {
            Convert(source []ID) []int
            Convert2(source IDs) []int
            Convert3(source IDs) IDs
            Convert4(source []ID) IDs
            Convert5(source IIDs) IDs
            Convert6(source []int) IIDs
            Convert7(source []int) IDs
        }
        type ID int
        type IDs []int
        type IIDs []ID
success: |
    // Code generated by github.com/jmattheis/go-genconv, DO NOT EDIT.

    package generated

    import execution "github.com/jmattheis/go-genconv/execution"

    type Converter struct{}

    func (c *Converter) Convert(source []execution.ID) []int {
    	targetSlice := make([]int, len(source))
    	for i := 0; i < len(source); i++ {
    		targetSlice[i] = int(source[i])
    	}
    	return targetSlice
    }
    func (c *Converter) Convert2(source execution.IDs) []int {
    	targetSlice := make([]int, len(source))
    	for i := 0; i < len(source); i++ {
    		targetSlice[i] = source[i]
    	}
    	return targetSlice
    }
    func (c *Converter) Convert3(source execution.IDs) execution.IDs {
    	targetSlice := make(execution.IDs, len(source))
    	for i := 0; i < len(source); i++ {
    		targetSlice[i] = source[i]
    	}
    	return targetSlice
    }
    func (c *Converter) Convert4(source []execution.ID) execution.IDs {
    	targetSlice := make(execution.IDs, len(source))
    	for i := 0; i < len(source); i++ {
    		targetSlice[i] = int(source[i])
    	}
    	return targetSlice
    }
    func (c *Converter) Convert5(source execution.IIDs) execution.IDs {
    	targetSlice := make(execution.IDs, len(source))
    	for i := 0; i < len(source); i++ {
    		targetSlice[i] = int(source[i])
    	}
    	return targetSlice
    }
    func (c *Converter) Convert6(source []int) execution.IIDs {
    	targetSlice := make(execution.IIDs, len(source))
    	for i := 0; i < len(source); i++ {
    		targetSlice[i] = execution.ID(source[i])
    	}
    	return targetSlice
    }
    func (c *Converter) Convert7(source []int) execution.IDs {
    	targetSlice := make(execution.IDs, len(source))
    	for i := 0; i < len(source); i++ {
    		targetSlice[i] = source[i]
    	}
    	return targetSlice
    }
