input:
    input.go: |
        package err

        // genconv:converter
        type Converter interface {
            Convert() map[string]int
        }
error: |-
    Error while creating converter method:
        func (github.com/jmattheis/go-genconv/execution.Converter).Convert() map[string]int

    expected signature to have only one parameter
