input:
    input.go: |
        package structs

        // goverter:converter
        type Converter interface {
            // goverter:matchIgnoreCase a
            ConvertString(source string) string
        }
error: |-
    error parsing 'goverter:matchIgnoreCase' at
        @workdir/input.go:6
        func (github.com/jmattheis/goverter/execution.Converter).ConvertString(source string) string

    invalid value: 'a' must be one of: yes, no
