input:
    input.go: |
        package execution

        // goverter:variables
        // goverter:output:format function
        var (
            Convert func(Input) Output
        )
        type Input struct {
            Name       string
        }
        type Output struct {
            Name       string
        }
error: |-
    error parsing 'goverter:output:format' at
        @workdir/input.go:5
        var definition

    unsupported format for goverter:variables
