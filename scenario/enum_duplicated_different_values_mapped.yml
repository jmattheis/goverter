input:
    input.go: |
        package example

        import (
            input "github.com/jmattheis/goverter/execution/input"
            output "github.com/jmattheis/goverter/execution/output"
        )

        // goverter:converter
        // goverter:enum:unknown @panic
        type Converter interface {
            // goverter:enum:map Default Gray
            Convert(input.Color) output.Color
        }
    input/enum.go: |
        package input

        type Color int

        const (
            Default = Gray
            Green Color = 1
            Gray  Color = 2
        )
    output/enum.go: |
        package output

        type Color string
        const (
            Default = Green
            Green Color = "green"
            Gray  Color = "gray"
        )
success:
    - generated/generated.go: |
        // Code generated by github.com/jmattheis/goverter, DO NOT EDIT.

        package generated

        import (
        	"fmt"
        	input "github.com/jmattheis/goverter/execution/input"
        	output "github.com/jmattheis/goverter/execution/output"
        )

        type ConverterImpl struct{}

        func (c *ConverterImpl) Convert(source input.Color) output.Color {
        	var outputColor output.Color
        	switch source {
        	case input.Default:
        		outputColor = output.Gray
        	// Skipped Gray(2) -> Gray(gray) because it duplicates Default(2) -> Gray(gray)
        	case input.Green:
        		outputColor = output.Green
        	default:
        		panic(fmt.Sprintf("unexpected enum element: %v", source))
        	}
        	return outputColor
        }
