input:
    input.go: |
        package err

        // goverter:converter
        // goverter:name a b
        type Converter interface {
        }
error: '/ABSOLUTE/execution/input.go:5:1: type Converter: invalid goverter:name must have one parameter'
