input:
    input.go: |
        package err

        type (
            // goverter:converter
            Converter interface {
                // goverter:map a b c
                ConvertString(source string) string
            }
        )
error: '/ABSOLUTE/execution/input.go:3:1: type Converter: parsing method ConvertString: invalid goverter:map too many fields'
