input:
    input.go: |
        package err

        // genconv:converter
        type Converter interface {
            Convert(map[string]int)
        }
error: |-
    Error while creating converter method: (github.com/jmattheis/go-genconv/execution.Converter).Convert

    return type must have at least one parameter
