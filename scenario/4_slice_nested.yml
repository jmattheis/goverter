input:
    input.go: |
        package slices

        // genconv:converter
        type Converter interface {
            Convert(source [][][]int) [][][]int
        }
success: |
    // Code generated by github.com/jmattheis/go-genconv, DO NOT EDIT.

    package generated

    type Converter struct{}

    func (c *Converter) Convert(source [][][]int) [][][]int {
    	targetSlice := make([][][]int, len(source))
    	for i := 0; i < len(source); i++ {
    		targetSlice2 := make([][]int, len(source[i]))
    		for j := 0; j < len(source[i]); j++ {
    			targetSlice3 := make([]int, len(source[i][j]))
    			for k := 0; k < len(source[i][j]); k++ {
    				targetSlice3[k] = source[i][j][k]
    			}
    			targetSlice2[j] = targetSlice3
    		}
    		targetSlice[i] = targetSlice2
    	}
    	return targetSlice
    }
