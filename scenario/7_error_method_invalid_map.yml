input:
    input.go: |
        package err

        type (
            // genconv:converter
            Converter interface {
                // genconv:map a b c
                ConvertString(source string) string
            }
        )
error: '/ABSOLUTE/execution/input.go:3:1: type Converter: parsing method ConvertString: invalid genconv:map must have two parameter'
