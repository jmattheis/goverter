input:
    input.go: |
        package structs

        type (
            // goverter:converter
            // goverter:
            Converter interface { }
        )
error: |-
    error parsing 'goverter:' at
        @workdir/input.go:6
        github.com/jmattheis/goverter/execution.Converter

    missing setting key
