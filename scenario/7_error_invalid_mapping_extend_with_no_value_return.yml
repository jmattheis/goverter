input:
    input.go: |
        package structs

        // goverter:converter
        type Converter interface {
            // goverter:mapExtend Age PluckAgeInt
            Convert(source Input) Output
        }

        type Input struct {
            Name string
            Age int
            Score int
        }
        type Output struct {
            Name string
            Age int
            Score int
        }
        func PluckAgeInt(source Input) {

        }

error: |-
    Error while parsing mapExtend in
    github.com/jmattheis/goverter/execution.Converter

    local package does not have mapExtend methods: PluckAgeInt

