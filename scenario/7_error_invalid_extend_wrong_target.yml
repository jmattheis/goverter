input:
    input.go: |
        package structs

        // goverter:converter
        // goverter:extend HelloWorld
        type Converter interface {
        }
        func HelloWorld(int) {
        }
error: |
    Error while parsing extend in
        github.com/jmattheis/goverter/execution.Converter

    HelloWorld has no or too many returns
