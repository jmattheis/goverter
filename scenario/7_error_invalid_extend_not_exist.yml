input:
    input.go: |
        package structs

        // goverter:converter
        // goverter:extend HelloWorld
        type Converter interface {
        }
error: 'Error while parsing extend methods: HelloWorld does not exist in scope'
