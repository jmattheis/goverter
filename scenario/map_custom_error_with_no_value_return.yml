input:
    input.go: |
        package structs

        // goverter:converter
        type Converter interface {
            // goverter:mapExtend Age PluckAgeInt
            Convert(source Input) Output
        }

        type Input struct {
            Name string
        }
        type Output struct {
            Name string
            Age int
        }
        func PluckAgeInt(source Input) {

        }
error: |-
    error parsing 'goverter:mapExtend' at
        /ABSOLUTE/execution/input.go:6:5
        func (github.com/jmattheis/goverter/execution.Converter).Convert(source github.com/jmattheis/goverter/execution.Input) github.com/jmattheis/goverter/execution.Output

    error parsing type:
        func github.com/jmattheis/goverter/execution.PluckAgeInt(source github.com/jmattheis/goverter/execution.Input)

    must have one or two returns
