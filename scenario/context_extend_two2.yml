input:
    input.go: |
        package example

        // goverter:converter
        // goverter:extend DoLookup
        type Converter interface {
            // goverter:context ctxOther
            // goverter:context ctxLookup
            Convert(source []string, ctxOther map[string]int, ctxLookup map[string]Output) []Output
        }

        // goverter:context ctx
        // goverter:context ctx2
        func DoLookup(ctx map[string]Output, id string, ctx2 map[string]int) Output {
            return ctx[id]
        }

        type Output struct {
            Name string
            Age int
        }
success:
    - generated/generated.go: |
        // Code generated by github.com/jmattheis/goverter, DO NOT EDIT.

        package generated

        import execution "github.com/jmattheis/goverter/execution"

        type ConverterImpl struct{}

        func (c *ConverterImpl) Convert(source []string, context map[string]int, context2 map[string]execution.Output) []execution.Output {
        	var exampleOutputList []execution.Output
        	if source != nil {
        		exampleOutputList = make([]execution.Output, len(source))
        		for i := 0; i < len(source); i++ {
        			exampleOutputList[i] = execution.DoLookup(context2, source[i], context)
        		}
        	}
        	return exampleOutputList
        }
