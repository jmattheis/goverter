input:
    input.go: |
        package structs

        // goverter:converter
        // goverter:extend HelloWorld
        type Converter interface {
        }
        const HelloWorld = 5
error: 'Error while parsing extend methods: HelloWorld is not a function'
